version: "3.9"

services:
  passportai:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./output:/app/output
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
